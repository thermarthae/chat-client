import { makeStyles } from '@material-ui/styles';

const errorStyles = makeStyles({
	root: {
		margin: 'auto',
		padding: 24,
		wordBreak: 'break-all'
	}
}, { name: 'Error' });
export default errorStyles;
